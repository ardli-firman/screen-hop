Task 03 - Config Load Test
===========================
Date: 2026-04-23

Test Command:
  python -c "from src.browser_move.config import load_config; c = load_config(); print(list(c.keys()))"

Output:
  ['presets', 'theme', 'auto_start']

Result: PASS
- load_config() returns dict with required keys
- Creates default config if missing
- Config path correctly resolves to project root (3 levels up from src/browser_move/config.py)
